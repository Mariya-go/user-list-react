import "./Loader.css";
import PropTypes from "prop-types";

const Loader = () => {
  return (
    <div className="ring">
      <span></span>
    </div>
  );
};

Loader.propTypes = {};

export default Loader;
